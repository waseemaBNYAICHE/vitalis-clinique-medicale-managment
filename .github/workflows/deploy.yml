name: CD - Google Cloud

on:
  workflow_run:
    workflows:
      - "VITALIS CI"
    types:
      - completed
    branches:
      - develop

  workflow_dispatch:

permissions:
  contents: read
  id-token: write

env:
  PROJECT_ID: vitalis-509611
  REGION: europe-west1
  REPOSITORY: vitalis-repo
  BACKEND_SERVICE: vitalis-backend
  FRONTEND_SERVICE: vitalis-frontend

jobs:
  deploy:
    name: Deploy VITALIS to Google Cloud
    runs-on: ubuntu-latest

    if: >
      github.event_name == 'workflow_dispatch' ||
      github.event.workflow_run.conclusion == 'success'

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4
        with:
          ref: develop

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: projects/148191533656/locations/global/workloadIdentityPools/github-pool/providers/github-provider
          service_account: github-cd@vitalis-509611.iam.gserviceaccount.com

      - name: Set up Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: |
          gcloud auth configure-docker europe-west1-docker.pkg.dev --quiet

      - name: Define image names
        run: |
          echo "BACKEND_IMAGE=europe-west1-docker.pkg.dev/${PROJECT_ID}/${REPOSITORY}/vitalis-backend:${GITHUB_SHA}" >> "$GITHUB_ENV"
          echo "FRONTEND_IMAGE=europe-west1-docker.pkg.dev/${PROJECT_ID}/${REPOSITORY}/vitalis-frontend:${GITHUB_SHA}" >> "$GITHUB_ENV"

      - name: Build backend Docker image
        run: |
          docker build \
            -f backend/Dockerfile.cloudrun \
            -t "$BACKEND_IMAGE" \
            backend

      - name: Push backend image
        run: |
          docker push "$BACKEND_IMAGE"

      - name: Deploy backend to Cloud Run
        run: |
          gcloud run deploy "$BACKEND_SERVICE" \
            --image="$BACKEND_IMAGE" \
            --region="$REGION" \
            --project="$PROJECT_ID" \
            --quiet

      - name: Get backend URL
        run: |
          BACKEND_URL=$(gcloud run services describe "$BACKEND_SERVICE" \
            --region="$REGION" \
            --project="$PROJECT_ID" \
            --format='value(status.url)')

          echo "BACKEND_URL=$BACKEND_URL" >> "$GITHUB_ENV"
          echo "Backend URL: $BACKEND_URL"

      - name: Build frontend Docker image
        run: |
          docker build \
            -f frontend/Dockerfile.prod \
            --build-arg VITE_API_URL="${BACKEND_URL}/api" \
            -t "$FRONTEND_IMAGE" \
            frontend

      - name: Push frontend image
        run: |
          docker push "$FRONTEND_IMAGE"

      - name: Deploy frontend to Cloud Run
        run: |
          gcloud run deploy "$FRONTEND_SERVICE" \
            --image="$FRONTEND_IMAGE" \
            --region="$REGION" \
            --project="$PROJECT_ID" \
            --quiet

      - name: Deployment summary
        run: |
          FRONTEND_URL=$(gcloud run services describe "$FRONTEND_SERVICE" \
            --region="$REGION" \
            --project="$PROJECT_ID" \
            --format='value(status.url)')

          echo "========================================"
          echo "VITALIS DEPLOYMENT SUCCESSFUL"
          echo "Backend  : $BACKEND_URL"
          echo "Frontend : $FRONTEND_URL"
          echo "========================================"
